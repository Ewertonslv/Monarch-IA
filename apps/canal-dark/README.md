# Canal Dark

CLI para produzir pautas e roteiros para canais dark.

## Uso

```bash
pip install -r requirements.txt
python -m canal_dark new --niche "historias" --audience "publico"
python -m canal_dark script --topic "Tema"
```
