# Monarch Web

Painel web atual do Monarch Hub.

Conteudo principal:

- `app.py`: backend FastAPI do painel
- `templates/hub.html`: interface principal do hub
- `templates/index.html`: template anterior mantido por compatibilidade

Observacao:

- O runtime atual ainda referencia `interfaces/web/` como compatibilidade.
- Esta pasta passa a ser o caminho canonico para evolucao da interface web.
