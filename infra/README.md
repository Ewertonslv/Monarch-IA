# Infra

Estrutura canonica de infraestrutura do repositorio:

- `deploy/`: arquivos de ambiente, nginx e documentacao operacional
- `scripts/`: bootstrap do servidor, deploy e rotinas de SSL

Os caminhos antigos `deploy/` e `scripts/` na raiz foram mantidos temporariamente como compatibilidade.
